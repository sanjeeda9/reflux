var React = require('react');
var MainLayout = require('../../components/Head');
var MovieBox = require('../../components/Load');
var Home = React.createClass({
  render () {
    return (
          <Head />
    );
  }
});

module.exports = Home;
